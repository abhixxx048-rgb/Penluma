---
// AUTO-GENERATED by scripts/gen-tool-pages.mjs — edit content there, not here.
import ToolLayout from '../../layouts/ToolLayout.astro';
import OnDeviceAiRewrite from '../../components/tools/OnDeviceAiRewrite.astro';
import TrustStrip from '../../components/TrustStrip.astro';

const faqs = [
    {
      "q": "Does my text get sent to a server?",
      "a": "No. This tool uses your browser’s built-in, on-device AI, so the rewrite happens locally. If your browser doesn’t support it yet, the tool tells you instead of falling back to a cloud service."
    },
    {
      "q": "Which browsers support built-in AI?",
      "a": "Support is new and rolling out gradually (notably in recent Chrome builds behind their built-in AI features). Where it isn’t available, try the WebGPU rewrite, which loads a model into the page instead."
    },
    {
      "q": "Is my text uploaded or stored anywhere?",
      "a": "No. Everything runs in your browser with plain JavaScript — your text is never uploaded, stored, or logged. Open your browser’s DevTools Network tab while you use it and you’ll see zero requests. The tool also works offline."
    }
  ];
---

<ToolLayout
  slug="on-device-ai-rewrite"
  title="On-Device AI Rewrite — Tone & Length Rewrites in Your Browser"
  description="One-click tone and length rewrites using your browser’s built-in AI — your text never leaves your device. Free on-device AI rewriter, experimental."
  keywords={["on device ai","browser ai rewrite","private ai rewrite","built-in ai rewriter"]}
  eyebrow="On-device AI · Free · In your browser"
  h1="On-Device AI Rewrite"
  lede="One-click <strong>tone and length rewrites</strong> using your browser’s built-in AI — so the text never leaves your device."
  definition="On-device AI rewrite uses a language model that runs inside your browser to rephrase text without sending it to any server."
  faqs={faqs}
  featureList={["Uses browser built-in AI","Tone and length presets","Side-by-side review","No cloud upload"]}
>
  <OnDeviceAiRewrite />

  <TrustStrip />

  <article class="seo mx-auto mt-16 max-w-3xl space-y-5 px-4 leading-relaxed">
    <p class="eyebrow text-mark">The short version</p>
    <hr class="rule-dash" />
    <h2 class="text-2xl font-bold">How to use it</h2>
    <ol class="list-decimal space-y-2 pl-6 muted">
        <li>Paste the passage you want to rewrite.</li>
        <li>Choose a tone or length target.</li>
        <li>Review the rewrite side by side and keep what works.</li>
    </ol>
    <h2 class="text-2xl font-bold">Private by design</h2>
    <p class="muted">
      There is no server and no upload. Everything runs with plain JavaScript on
      your device, so you can safely work with confidential or unpublished text.
      Open your browser's DevTools Network tab while you use it and you'll see
      zero requests — and it keeps working with your connection turned off.
    </p>
  </article>
</ToolLayout>
