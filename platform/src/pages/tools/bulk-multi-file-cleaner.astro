---
// AUTO-GENERATED by scripts/gen-tool-pages.mjs — edit content there, not here.
import ToolLayout from '../../layouts/ToolLayout.astro';
import BulkMultiFileCleaner from '../../components/tools/BulkMultiFileCleaner.astro';
import TrustStrip from '../../components/TrustStrip.astro';

const faqs = [
    {
      "q": "How do I clean multiple files at once?",
      "a": "Drop them all onto the tool. Each file is read and cleaned in your browser with the options you choose, then offered back for download — nothing is uploaded to a server."
    },
    {
      "q": "What file types are supported?",
      "a": "Plain-text formats like .txt and .md work best. The cleaner reads the text content in your browser and applies the same pass you’d run on a single paste."
    },
    {
      "q": "Is my text uploaded or stored anywhere?",
      "a": "No. Everything runs in your browser with plain JavaScript — your text is never uploaded, stored, or logged. Open your browser’s DevTools Network tab while you use it and you’ll see zero requests. The tool also works offline."
    }
  ];
---

<ToolLayout
  slug="bulk-multi-file-cleaner"
  title="Bulk / Multi-File Cleaner — Clean Many Files at Once"
  description="Drop multiple text files, clean each one in your browser, and download the results in a batch. No upload, no signup. Free bulk text cleaner, in-browser."
  keywords={["bulk text cleaner","clean multiple files","batch text cleanup","multi file cleaner"]}
  eyebrow="Power tool · Free · In your browser"
  h1="Bulk / Multi-File Cleaner"
  lede="Drop <strong>multiple files at once</strong>, clean each one in your browser, and download the results in a batch — no upload, no queue."
  definition="A bulk cleaner applies the same text-cleanup pass to many files at once, entirely on your device."
  faqs={faqs}
  featureList={["Batch multiple files","Same cleanup options across all","Per-file download","Nothing leaves the browser"]}
>
  <BulkMultiFileCleaner />

  <TrustStrip />

  <article class="seo mx-auto mt-16 max-w-3xl space-y-5 px-4 leading-relaxed">
    <p class="eyebrow text-mark">The short version</p>
    <hr class="rule-dash" />
    <h2 class="text-2xl font-bold">How to use it</h2>
    <ol class="list-decimal space-y-2 pl-6 muted">
        <li>Drag in the files you want to clean.</li>
        <li>Pick the cleanup options to apply to all of them.</li>
        <li>Download the cleaned files individually or together.</li>
    </ol>
    <h2 class="text-2xl font-bold">Private by design</h2>
    <p class="muted">
      There is no server and no upload. Everything runs with plain JavaScript on
      your device, so you can safely work with confidential or unpublished text.
      Open your browser's DevTools Network tab while you use it and you'll see
      zero requests — and it keeps working with your connection turned off.
    </p>
  </article>
</ToolLayout>
