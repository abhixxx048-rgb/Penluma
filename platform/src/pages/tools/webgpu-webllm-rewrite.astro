---
// AUTO-GENERATED by scripts/gen-tool-pages.mjs — edit content there, not here.
import ToolLayout from '../../layouts/ToolLayout.astro';
import WebgpuWebllmRewrite from '../../components/tools/WebgpuWebllmRewrite.astro';
import TrustStrip from '../../components/TrustStrip.astro';

const faqs = [
    {
      "q": "What is an in-browser LLM with WebGPU?",
      "a": "WebGPU lets web pages use your GPU, which makes it possible to run a compact language model directly in the browser. The model weights download once and cache; after that, rewrites run locally with no server involved."
    },
    {
      "q": "Why is it slower the first time?",
      "a": "The model has to download and initialize on first use, which can take a moment and a few hundred MB. Subsequent runs use the cached model and are much faster."
    },
    {
      "q": "Is my text uploaded or stored anywhere?",
      "a": "No. Everything runs in your browser with plain JavaScript — your text is never uploaded, stored, or logged. Open your browser’s DevTools Network tab while you use it and you’ll see zero requests. The tool also works offline."
    }
  ];
---

<ToolLayout
  slug="webgpu-webllm-rewrite"
  title="WebGPU Rewrite — Experimental On-Device Rewrite That Never Uploads"
  description="Run a language model directly in your browser with WebGPU to rewrite text locally — nothing is uploaded. Free, experimental on-device rewriter."
  keywords={["webgpu llm","in browser llm","webllm rewrite","private ai rewrite"]}
  eyebrow="On-device AI · Free · In your browser"
  h1="WebGPU Rewrite"
  lede="Load a language model <strong>directly into the page with WebGPU</strong> and rewrite text locally — your words never leave the browser."
  definition="A WebGPU rewrite runs an actual LLM on your device’s GPU inside the browser, with no server round-trip."
  faqs={faqs}
  featureList={["Runs an LLM via WebGPU","Model cached after first load","Fully local inference","Nothing uploaded"]}
>
  <WebgpuWebllmRewrite />

  <TrustStrip />

  <article class="seo mx-auto mt-16 max-w-3xl space-y-5 px-4 leading-relaxed">
    <p class="eyebrow text-mark">The short version</p>
    <hr class="rule-dash" />
    <h2 class="text-2xl font-bold">How to use it</h2>
    <ol class="list-decimal space-y-2 pl-6 muted">
        <li>Let the model download and initialize (one time, cached).</li>
        <li>Paste your text and choose how to rewrite it.</li>
        <li>Review the local rewrite — nothing was uploaded.</li>
    </ol>
    <h2 class="text-2xl font-bold">Private by design</h2>
    <p class="muted">
      There is no server and no upload. Everything runs with plain JavaScript on
      your device, so you can safely work with confidential or unpublished text.
      Open your browser's DevTools Network tab while you use it and you'll see
      zero requests — and it keeps working with your connection turned off.
    </p>
  </article>
</ToolLayout>
